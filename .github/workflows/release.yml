name: Build and Release Extension

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version tag (e.g., v2.0.2)'
        required: true

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: client/package-lock.json

      - name: Install dependencies
        working-directory: client
        run: npm ci

      - name: Build extension
        working-directory: client
        run: npm run build

      - name: Create Chrome zip artifact
        env:
          VERSION: ${{ github.event.inputs.version || github.ref_name }}
        run: |
          cd client/extension
          zip -r "../../wit-calendar-${VERSION}.zip" .

      - name: Patch Firefox build
        working-directory: client
        run: npm run patch-firefox

      - name: Create Firefox zip artifact
        env:
          VERSION: ${{ github.event.inputs.version || github.ref_name }}
        run: |
          cd client/extension
          zip -r "../../wit-calendar-${VERSION}-firefox.zip" .

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.event.inputs.version || github.ref_name }}
          files: |
            wit-calendar-${{ github.event.inputs.version || github.ref_name }}.zip
            wit-calendar-${{ github.event.inputs.version || github.ref_name }}-firefox.zip
          generate_release_notes: true
